# Go test/benchmark/fuzz functions deliberately removed from the tree, one per
# line, as "<FuncName>  # <file> :: <grep target> :: <bucket>".
#
# The inventory gate subtracts the number of entries here from the expected
# total. Adding an entry is how a deliberate deletion is itemized; a removal
# that is not listed here fails the gate as an unexplained loss.
#
# Blank lines and lines starting with '#' are ignored.

TestLatin1DecodeDocCommentRewritten  # tests/pdf-parsing-correctness-fixes/correctness_fixes_test.go :: latin1Decode doc-comment block in internal/pdfcore/plaintext.go ("C1 controls", "0x80-0x9F", "verbatim") :: comment-grep
TestGetPlainTextSizeDocCommentUpdated  # tests/pdf-parsing-correctness-fixes/correctness_fixes_test.go :: GetPlainTextSize doc-comment block in internal/pdfcore/plaintext.go ("surfaces the raw os.Stat error", "captured at Open") :: comment-grep
TestReverseRefIndexBuiltAtOpen  # tests/object-source-and-reverse-refs/object_source_and_reverse_refs_test.go :: "buildReverseRefs" in internal/pdfcore/inspector.go, where it now occurs in comments only :: comment-grep
